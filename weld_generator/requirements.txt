# Tier 1 core (D9): no simulator, no GPU, no ROS. Must install on a clean machine.
numpy>=1.24
trimesh>=4.0
PyYAML>=6.0

# Validation and tests
jsonschema>=4.20
pytest>=7.0

# Notebooks / inspection only
matplotlib>=3.7

# Phase 3. Ray-cast visibility does NOT need a ray engine: every part is a box, so
# ray-box intersection is exact in closed form (weldgen/visibility.py). `trimesh.ray`
# would have pulled in `rtree`, which D9 rules out. scipy is only for the HPR convex
# hull, and it already ships as a trimesh dependency.
scipy>=1.10

# Phase 6 introduces swept and revolved primitives, where a real ray engine earns itself.
# open3d>=0.18
