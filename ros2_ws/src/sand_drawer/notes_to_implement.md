
lets make trajectory generation method that project a randomly generated trajectory that follows a certain rule of pattern (some kind of Zen shape generation)

lets make trajectory generation method that project a randomly generated trajectory that follows a certain rule of pattern (basic RRT repsresentation to a random start and end point)

Gui based trajectory generation that takes line drawing as input and generates trajectory for the robot to follow. This can be done using a simple GUI where the user can draw a line and the system will convert that line into a trajectory for the robot to follow. qt based maybe.